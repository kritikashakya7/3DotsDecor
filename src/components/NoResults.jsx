const NoResults = ({ text }) => {
  return (
    <div className="min-height flex items-center justify-center">
      <h1 className="text-center">{text}</h1>
    </div>
  );
};

export default NoResults;
